{# Iterate through the terms #}
{% if terms %}
  <ul>
    {% for term in terms %}
      <li>{{ term.name }}</li>
      {# You can access other properties like term.id, term.parent, etc. as required #}
    {% endfor %}
  </ul>
{% else %}
  <p>No terms found.</p>
{% endif %}
